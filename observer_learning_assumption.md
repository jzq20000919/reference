# 分布式观测器与学习数据方程：理论极限和实际采样的区别

## 目的

本文采用分布式观测器估计领导者状态 \(v\) 及其系统矩阵 \(S\)。
在数据驱动 HI 学习开始前，观测器先经历一个收敛暂态。必须区分：

1. 用于定理推导的**理论极限模型**；
2. 用于数值实现的**有限时间近似模型**。

这一区分避免将“渐近收敛”误写为“在有限时间内严格相等”。

## 观测器和速率收敛

令 \(\zeta_0=v\)、\(S_0=S\)，并对每个跟随者实施

\[
\dot S_i=\mu_1\sum_{j=0}^{N}a_{ij}(S_j-S_i),
\]

\[
\dot\zeta_i=S_i\zeta_i+
\mu_2\sum_{j=0}^{N}a_{ij}(\zeta_j-\zeta_i).
\]

记 \(\tilde S_i=S_i-S\)、\(\tilde\zeta_i=\zeta_i-v\)。若 follower 子图无环，且 \(H\) 的全部特征值具有正实部，令

\[
\alpha_H=\min_{\lambda\in\sigma(H)}\operatorname{Re}(\lambda).
\]

对于给定的速率 \(\gamma\ge0\)，若

\[
\mu_1\alpha_H>\gamma
\]

并且

\[
I_N\otimes(S+\gamma I)-\mu_2H\otimes I
\]

为 Hurwitz 矩阵，则

\[
\lim_{t\to\infty}e^{\gamma t}\tilde S_i(t)=0,
\qquad
\lim_{t\to\infty}e^{\gamma t}\tilde\zeta_i(t)=0.
\]

一个便于使用的充分条件为

\[
\mu_2>
\frac{\gamma+\max_{\lambda\in\sigma(S)}\operatorname{Re}(\lambda)}
{\alpha_H}.
\]

当 \(S\) 的特征值实部均为零时，此条件简化为
\(\mu_2>\gamma/\alpha_H\)。

等价地，定义 \(\zeta_i^\gamma=e^{\gamma t}\zeta_i\) 和
\(v^\gamma=e^{\gamma t}v\)，则

\[
\zeta_i^\gamma-v^\gamma=e^{\gamma t}(\zeta_i-v)\to0.
\]

## 两种模型的严格区别

| 项目 | 理论极限模型 | 有限时间实际采样模型 |
|---|---|---|
| 使用的领导者状态 | 令 \(\zeta_i=v\) | 使用 \(\zeta_i=v+\tilde\zeta_i\) |
| 使用的领导者矩阵 | 令 \(S_i=S\) | 使用 \(S_i=S+\tilde S_i\) |
| 数据方程 | 精确线性方程 | 带 \(\tilde S_i,\tilde\zeta_i\) 引起的残差项 |
| 初始/后续秩条件 | 对理想数据矩阵提出 | 实际中检查采样数据矩阵的数值秩或最小奇异值 |
| HI 收敛结论 | 可证明与模型 VI/PI 等价，并收敛到理想最优解 | 仅能说明结果接近理想迭代；需额外扰动/噪声鲁棒性分析才能给出误差界 |
| 有限时间是否可写“等于” | 仅作为极限分析的理想条件 | 不可以；普通渐近观测器一般不会在有限时间严格满足 \(\zeta_i=v\) |

## 本文建议采用的写法

### 理论部分

先给出观测器速率收敛引理，证明

\[
e^{\gamma t}(S_i-S)\to0,
\qquad
e^{\gamma t}(\zeta_i-v)\to0.
\]

随后声明：为建立无噪声 HI 的精确数据方程及其收敛性，学习阶段在观测器暂态消失的极限模型下分析。因此，在学习定理中采用 \(S_i=S\)、\(\zeta_i=v\)。

这一声明的含义是：学习算法的理论结论针对理想数据方程，而不是声称有限时间观测误差严格为零。

### 算法与实施部分

实际执行时，先运行两个分布式观测器。在满足预设阈值后再开始采样，例如

\[
\|S_i-S_j\|\le \varepsilon_S,
\qquad
\|\zeta_i-\zeta_j\|\le\varepsilon_\zeta
\]

对可通信的邻居成立，或在仿真中直接检查 \(\|S_i-S\|\) 与
\(\|\zeta_i-v\|\)。此时实际数据方程是理想方程的近似。

如果本文不额外建立含观测残差的 HI 鲁棒收敛定理，则应避免使用以下表述：

> 观测器在有限时间收敛，因此学习数据中的 \(\zeta_i\) 严格等于 \(v\)。

建议替换为：

> 由于分布式观测器以高于预设速率 \(\gamma\) 的指数速度收敛，数据采集在观测器暂态充分衰减后启动。HI 的理论分析基于观测器极限下的理想数据方程；有限时间观测残差在实际实施中作为小扰动处理。

## 若未来需要更强的结论

若要证明实际有限时间学习增益与理想最优增益之间的定量误差界，需要补充：

1. 数据矩阵满秩在小扰动下保持的最小奇异值下界；
2. 最小二乘解相对于观测器残差的扰动界；
3. VI--PI 迭代对每步数据方程误差的鲁棒性分析；
4. 最终增益误差和加权调节误差的上界。

这属于扩展的鲁棒数据驱动学习问题，不是本文证明理想 HI 收敛性的必要前提。
